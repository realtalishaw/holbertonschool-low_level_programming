#include "holberton.h"

/**
* print_rev - ekdjlsk
* @s: ejsekf
*
* Return: fjkhdsk
*/

void print_rev(char *s)
{
	int len, i;

	for (len  = 0; s[len]; len++)
	{
		;

	}
	for (i = len; i >= 0; i--)
		_putchar(s[i]);
	_putchar('\n');
}
